<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pokémon</title>
    <style>
        h1 {
            font-size: 5rem;
        }

        label {
            font-size: 3rem;
        }

        input {
            font-size: 3rem;
        }

        button {
            font-size: 2rem;
        }

        a {
            font-size: 1.5rem;
        }
    </style>
</head>

<body>
    <h1>Edit Pokémon</h1>
    <form action="/edit/<%= pokemon.id %>" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= pokemon.name %>" required>
        <br>
        <label for="level">Level:</label>
        <input type="number" name="level" id="level" value="<%= pokemon.level %>" required>
        <br>
        <label for="type">Type:</label>
        <input type="text" name="type" id="type" value="<%= pokemon.type %>" required>
        <br>
        <button type="submit">Update Pokémon</button>
    </form>
    <a href="/">Back to Pokémon List</a>
</body>

</html>